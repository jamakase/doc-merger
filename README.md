# Система оценки заявлений на поступление с использованием CrewAI

Эта система использует фреймворк CrewAI для автоматической оценки заявлений студентов на поступление в университет. Система анализирует документы студентов и оценивает их по заданным критериям.

## Архитектура системы

Система использует **Direct Code Definition** подход CrewAI и состоит из трех специализированных агентов:

1. **Document Processor** - обрабатывает и извлекает информацию из документов студентов
2. **Criteria Analyst** - анализирует критерии оценки из файла criteria.md  
3. **Admission Evaluator** - выполняет итоговую оценку на основе обработанных данных

### Преимущества Direct Code Definition:
- ✅ Не требует YAML конфигурационных файлов
- ✅ Полный контроль над созданием агентов и задач
- ✅ Простая отладка и модификация
- ✅ Прозрачность кода и логики

## Установка

1. Установите зависимости:
```bash
pip install -r requirements.txt
```

2. Настройте переменные окружения (создайте файл .env):
```
OPENAI_API_KEY=your_openai_api_key_here
```

## Структура проекта

```
commission/
├── agent.py               # Основная логика CrewAI системы
├── main.py               # Главный скрипт для запуска
├── requirements.txt      # Зависимости Python
├── criteria.md          # Критерии оценки (создается автоматически)
├── student_documents/   # Директория с документами студентов
└── README.md           # Этот файл
```

## Использование

### Быстрый старт:

```bash
# Запуск примера
python run_example.py
```

### Основной способ запуска:

```bash
python main.py [путь_к_папке_с_документами]
```

Если путь не указан, система будет искать документы в папке `./student_documents`.

### Примеры:

```bash
# Быстрый пример с автоматической настройкой
python run_example.py

# Использование директории по умолчанию
python main.py

# Указание конкретной директории
python main.py ./applicant_john_doe/

# Прямое использование в коде
python agent.py
```

## Подготовка данных

1. **Создайте директорию с документами студента:**
   ```bash
   mkdir student_documents
   ```

2. **Поместите туда документы в форматах:**
   - PDF файлы (.pdf)
   - Word документы (.docx)

3. **Критерии оценки** автоматически создадутся в файле `criteria.md` при первом запуске.

## Пример использования в коде

```python
from agent import AdmissionEvaluationCrew

# Создание системы оценки
evaluation_system = AdmissionEvaluationCrew()

# Запуск оценки для директории с документами
result = evaluation_system.evaluate_application("./student_documents")

print(result)
```

## Структура результата

Система выдает детализированный отчет включающий:

1. **Индивидуальные оценки** по каждой категории критериев
2. **Обоснование** для каждой выставленной оценки
3. **Общий взвешенный балл**
4. **Анализ сильных и слабых сторон**
5. **Финальную рекомендацию** (Принять/Отклонить/Список ожидания)
6. **Обоснование** финального решения

## Настройка критериев

Отредактируйте файл `criteria.md` для изменения критериев оценки:

- Академическая успеваемость (40% веса)
- Исследовательский опыт и публикации (30% веса)  
- Профессиональный опыт (20% веса)
- Награды и достижения (10% веса)

## Возможности системы

- ✅ **Многоагентная архитектура** с разделением ответственности
- ✅ **Последовательная обработка** задач через Process.sequential
- ✅ **Автоматическое извлечение** информации из документов
- ✅ **Гибкие критерии** оценки через YAML/Markdown конфигурацию
- ✅ **Детализированные отчеты** с обоснованиями
- ✅ **Сохранение результатов** в файл evaluation_result.md

## Отладка и мониторинг

Система включает подробное логирование:
- Установите `verbose=True` в конфигурации агентов
- Все действия агентов отображаются в консоли
- Результаты сохраняются в файл для дальнейшего анализа

## Требования к API

Для работы системы требуется:
- OpenAI API ключ для работы LLM моделей
- Подключение к интернету для CrewAI инструментов

## Document Extractor & PDF Viewer

В проект добавлен модуль для извлечения документов из архивов и их просмотра:

### Быстрый запуск (рекомендуется)

Используйте Makefile для удобного управления проектом:

```bash
# Показать все доступные команды
make help

# Установить все зависимости
make install

# Запустить и фронтенд, и бэкенд одновременно
make dev

# Запустить только API сервер
make dev-backend

# Запустить только фронтенд
make dev-frontend

# Остановить все сервисы
make stop
```

### Альтернативный запуск

#### API Сервер (FastAPI)

```bash
# Установка зависимостей
pip install -r requirements.txt

# Запуск API сервера
python start_api.py
```

API будет доступен по адресу: http://localhost:8000
Документация API: http://localhost:8000/docs

#### Фронтенд (Next.js)

```bash
cd my-app
npm install
npm run dev
```

Веб-интерфейс будет доступен по адресу: http://localhost:3000

### Возможности Document Extractor:

- ✅ **Извлечение документов** из ZIP, RAR, TAR архивов
- ✅ **Конвертация в PDF** или текстовый формат
- ✅ **Объединение документов** в один файл
- ✅ **Веб-интерфейс** для загрузки и просмотра
- ✅ **PDF Viewer** встроенный в браузер
- ✅ **Отслеживание статуса** обработки
- ✅ **Скачивание результатов** в удобном формате

### Команды Makefile:

| Команда | Описание |
|---------|----------|
| `make` или `make help` | Показать справку по командам |
| `make install` | Установить все зависимости (Python + Node.js) |
| `make dev` | 🔥 **Запустить оба сервиса одновременно** |
| `make dev-backend` | Запустить только FastAPI сервер |
| `make dev-frontend` | Запустить только Next.js фронтенд |
| `make stop` | Остановить все запущенные сервисы |
| `make build` | Собрать фронтенд для продакшена |
| `make clean` | Очистить временные файлы |
| `make check` | Проверить системные зависимости |

**Псевдонимы**: `start`, `server`, `client`, `frontend`, `backend`, `api`

### API Endpoints:

- `POST /extract` - Начать извлечение документов
- `GET /status/{task_id}` - Проверить статус задачи
- `GET /download/{task_id}` - Скачать результат
- `GET /health` - Проверка работоспособности

## Лицензия

Этот проект создан для образовательных целей в рамках университетского курса.
